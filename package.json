{
  "name": "mtb-standalone",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --workspace server\" \"npm.cmd run dev --workspace client\"",
    "build": "npm.cmd run build --workspace server && npm.cmd run build --workspace client",
    "db:migrate": "npm.cmd run db:migrate --workspace server",
    "db:seed": "npm.cmd run db:seed --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2"
  }
}
